# SPENDLY — requirements.txt
# Install with:  pip install -r requirements.txt
#
# Versions are pinned with >= so teammates get compatible installs
# without being locked to an exact build.  Tighten pins for production.

# Core data stack
pandas>=2.0.0
numpy>=1.24.0

# Machine learning utilities (used in preprocess.py)
scikit-learn>=1.3.0

# Visualisation (used in EDA notebook)
matplotlib>=3.7.0
seaborn>=0.12.0

# Jupyter notebook (for 01_EDA.ipynb)
jupyter>=1.0.0
ipykernel>=6.0.0

# Deep learning — Person 4 (LSTM forecasting)
# tensorflow>=2.13.0        # or torch>=2.0.0 if Person 4 prefers PyTorch
torch>=2.0.0

# Dashboard — Person 3 / Person 4
streamlit>=1.28.0

# Optional but recommended: faster CSV I/O
pyarrow>=13.0.0
